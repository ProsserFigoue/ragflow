services:
  es01:
    image: docker.elastic.co/elasticsearch/elasticsearch:${STACK_VERSION}
    environment:
      - node.name=es01
      - ELASTIC_PASSWORD=${ELASTIC_PASSWORD}
      - discovery.type=single-node
      - xpack.security.enabled=true
      - xpack.security.http.ssl.enabled=false
      - xpack.security.transport.ssl.enabled=false
      - "ES_JAVA_OPTS=-Xms2g -Xmx2g"
    volumes:
      - esdata01:/usr/share/elasticsearch/data
    networks:
      - ragflow
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200 >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  mysql:
    image: mysql:8.0.39
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DBNAME}
      - TZ=${TZ}
    command:
      - --max_connections=1000
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_general_ci
      - --default-authentication-plugin=mysql_native_password
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - ragflow
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-uroot", "-p${MYSQL_PASSWORD}"]
      interval: 10s
      timeout: 10s
      retries: 3

  minio:
    image: quay.io/minio/minio:RELEASE.2023-12-20T01-00-02Z
    environment:
      - MINIO_ROOT_USER=${MINIO_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_PASSWORD}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    networks:
      - ragflow
    restart: unless-stopped

  redis:
    image: redis:7.2.4
    command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 128mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - ragflow
    restart: unless-stopped

  ragflow-cpu:
    depends_on:
      mysql:
        condition: service_healthy
      es01:
        condition: service_healthy
    image: ${RAGFLOW_IMAGE}
    command:
      - --enable-adminserver
    # CONFIGURATION RÉSEAU 10/10 : Pas de ports exposés sur l'hôte, routage interne pur.
    labels:
      - "coolify.proxy.port=80"
      - "traefik.enable=true"
      - "traefik.http.routers.${COOLIFY_CONTAINER_NAME}.entrypoints=websecure"
      - "traefik.http.routers.${COOLIFY_CONTAINER_NAME}.tls=true"
      - "traefik.http.services.${COOLIFY_CONTAINER_NAME}.loadbalancer.server.port=80"
    volumes:
      - ragflow_logs:/ragflow/logs
    env_file: .env
    networks:
      - ragflow
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "timeout 1s bash -c ': </dev/tcp/127.0.0.1/80' || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10

volumes:
  esdata01:
  mysql_data:
  minio_data:
  redis_data:
  ragflow_logs:

networks:
  ragflow:
    driver: bridge
